<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_notice_view"
    android:padding="12dp">

    <ImageView
        android:id="@+id/iv_notice_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentLeft="true"
        android:src="@drawable/ic_notice_icon" />

    <TextView
        android:id="@+id/tv_notice_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/iv_notice_icon"
        android:layout_alignBottom="@+id/iv_notice_icon"
        android:layout_toRightOf="@+id/iv_notice_icon"
        android:gravity="center_vertical"
        android:paddingLeft="10dp"
        android:text="云集"
        android:textColor="@android:color/black"
        android:textSize="10sp" />

    <TextView
        android:id="@+id/tv_notice_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_notice_icon"
        android:paddingTop="4dp"
        android:text="这是一个站内信的内容，您可以传入你的数据"
        android:textColor="@android:color/black"
        android:textSize="12sp" />

</RelativeLayout>